spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/elizkurtulus/smart-order.git
          default-label: master
          search-paths: 
            - configurations/
            - configurations/{application}

server:
  port: 8787